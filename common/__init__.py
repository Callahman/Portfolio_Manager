"""Shared utilities for Portfolio_Manager packages.

`common/` is deliberately OUT of scope for role-scoped self-modification
(same as portfolio/, delivery/, api/, team/): it holds the config loader,
canonical paths, and the structured failure-report contract.
"""

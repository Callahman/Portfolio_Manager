"""LLM team runtime (Step 5, 4090) — role definitions, pod manager,
ad-hoc sessions, role-scoped self-modification loop.

Runs on the 4090 in the DeepSeek harness (Qwen 3.8). OUT of scope for
self-modification by construction (the loop modifies collect/validate/
analyze only).
"""

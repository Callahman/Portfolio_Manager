"""Read-only LAN HTTP API (Step 4) — served on the 1080 while awake.

GET /feeds/{role} /quality /failures /history; POST /recommendation (the
only write). OUT of scope for self-modification by construction.
"""

"""Validation package (Step 2) — deterministic quality checks into
warehouse/validated (its own schema) + data quality report + structured
failure reports (input to the self-modification loop, outline §2.9).

Self-mod scope: ETL (validate/) — Data Engineer + Data Scientist.
"""
